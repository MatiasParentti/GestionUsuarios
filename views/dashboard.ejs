<!DOCTYPE html>
<html>
<head>
  <title>Dashboard</title>

        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
</head>
<body class="bg-body-tertiary">
 

<div class="container mt-4">
 <nav class="navbar bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand ">Bienvenido</a>
    <form class="d-flex" role="search">
   
       <span id="userInfo" class="navbar-text me-2 fs-5 fst-italic">
      
    </span>
      <button class="btn btn-outline-danger" type="button" id="logout">Cerrar Sesion</button>
    </form>
  </div>
</nav>

<div class="card mt-2">
  <div class="card-header">
    Gestion de Usuarios 
  </div>
  <div class="card-body">
    <figure>
      <blockquote class="blockquote">
        <p>Administracion general del sistema</p>
      </blockquote>
      <figcaption class="blockquote-footer">
        Someone famous in <cite title="Source Title">Source Title</cite>
      </figcaption>
    </figure>
  </div>
</div>

<ul class="list-group list-group-flush col-4 mt-2">
  <li class="list-group-item fs-5"><a href="/users">Usuarios</a></li>
  <li class="list-group-item fs-5"><a href="/roles">Roles</a></li>
  <li class="list-group-item fs-5"><a href="/permisos">Permisos</a></li>
</ul>


  

</div>


  <script>
    async function fetchUser() {
      const res = await fetch('/auth/me', {
        credentials: 'include',
      });

      if (res.ok) {
        const data = await res.json();
        document.getElementById('userInfo').innerText = `Usuario autenticado: ${data.username}`;
      } else {
        document.getElementById('userInfo').innerText = 'No hay sesión activa';
      }
    }

    document.getElementById('logout').addEventListener('click', async () => {
      await fetch('/auth/logout', {
        method: 'POST',
        credentials: 'include',
      });
      window.location.href = '/login';
    });

    fetchUser();
  </script>
</body>
</html>
